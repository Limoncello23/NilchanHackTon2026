# Just use mock API (default when unset or true) until we have a live Go backend. Set to false for live Go backend.
NEXT_PUBLIC_USE_MOCK=true

# Live API base URL (used when NEXT_PUBLIC_USE_MOCK=false)
NEXT_PUBLIC_API_URL=http://localhost:8080
